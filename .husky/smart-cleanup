#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧠 Running Smart File Manager pre-commit cleanup..."

# Run smart file manager in non-interactive mode for commit cleanup
node scripts/smart-file-manager.js --no-watch --verbose

# Also run the existing cleanup
node scripts/cleanup-old-files.js --verbose

echo "✅ Smart cleanup completed!"
