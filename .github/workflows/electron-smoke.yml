name: Electron Smoke (Ubuntu)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - "apps/terminal-pro/**"
      - "deploy/**"
      - ".github/workflows/electron-smoke.yml"
      - "package.json"
      - "package-lock.json"
  pull_request:
    paths:
      - "apps/terminal-pro/**"
      - "deploy/**"
      - ".github/workflows/electron-smoke.yml"
      - "package.json"
      - "package-lock.json"

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Check duplicate IPC channels
        run: pnpm --filter rinawarp-terminal-pro run check:ipc

      - name: Lint (ESLint)
        run: pnpm --filter rinawarp-terminal-pro run lint -- --format json --output-file /tmp/eslint.json || true

      - name: Enforce warning budget
        run: node scripts/eslint-warning-budget.js --max 0 /tmp/eslint.json

      - name: Clean build outputs
        run: pnpm run clean

      - name: Build Electron app
        run: pnpm --filter rinawarp-terminal-pro run build:electron

      - name: Check repo hygiene
        run: ./scripts/check-repo-hygiene.sh

      - name: Enforce shared E2E launch helper
        run: ./scripts/check-e2e-launch.sh

      - name: Run Playwright smoke under Xvfb (CI sandbox disabled)
        env:
          CI: "1"
          RINAWARP_E2E: "1"
          ELECTRON_DISABLE_SANDBOX: "1"
        run: xvfb-run -a pnpm --filter rinawarp-terminal-pro run e2e:smoke

      - name: Run Electron proof and trust suite
        env:
          CI: "1"
          RINAWARP_E2E: "1"
          ELECTRON_DISABLE_SANDBOX: "1"
        run: pnpm --filter rinawarp-terminal-pro run test:e2e:core

      - name: Build CI failure bundle
        if: failure()
        env:
          CI: "1"
          RINAWARP_E2E: "1"
          ELECTRON_DISABLE_SANDBOX: "1"
        run: |
          xvfb-run -a node scripts/collect-ci-support-artifacts.mjs artifacts || true
          ./scripts/create-ci-failure-bundle.sh

      - name: Upload Playwright artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: electron-smoke-artifacts
          path: |
            artifacts/*.tgz
            artifacts/diagnostics.json
            artifacts/support-bundle-result.json
            artifacts/support-bundle.zip
            apps/terminal-pro/test-results
            apps/terminal-pro/playwright-report
          if-no-files-found: ignore
